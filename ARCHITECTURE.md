# 🏗️ Kiến Trúc Hệ Thống (System Architecture)

## 📊 Sơ Đồ Tổng Quan

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              React Frontend (Port 3000)                │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐            │ │
│  │  │  Login   │  │ Courses  │  │ Profile  │            │ │
│  │  └──────────┘  └──────────┘  └──────────┘            │ │
│  │  ┌──────────┐  ┌──────────┐                          │ │
│  │  │ Register │  │MyCourses │                          │ │
│  │  └──────────┘  └──────────┘                          │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP/HTTPS
                            │ (Axios API Calls)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    SERVER (Ubuntu/Windows)                   │
│  ┌────────────────────────────────────────────────────────┐ │
│  │         Express.js Backend (Port 5000)                 │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │            API Routes                            │ │ │
│  │  │  • POST /api/auth/register                       │ │ │
│  │  │  • POST /api/auth/login                          │ │ │
│  │  │  • GET  /api/courses                             │ │ │
│  │  │  • POST /api/courses/:id/enroll                  │ │ │
│  │  │  • DELETE /api/courses/:id/unenroll              │ │ │
│  │  │  • GET  /api/users/profile                       │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │         Middleware                               │ │ │
│  │  │  • JWT Authentication                            │ │ │
│  │  │  • CORS                                          │ │ │
│  │  │  • Error Handling                                │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Mongoose ODM
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  DATABASE (Port 27017)                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                   MongoDB                              │ │
│  │  ┌──────────────┐  ┌──────────────┐                  │ │
│  │  │    users     │  │   courses    │                  │ │
│  │  │  Collection  │  │  Collection  │                  │ │
│  │  └──────────────┘  └──────────────┘                  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Luồng Dữ Liệu (Data Flow)

### 1. Đăng Ký (Registration)

```
User Input (Form)
    │
    ├── Name
    ├── Email
    ├── Student ID
    └── Password
        │
        ▼
    Validation (Frontend)
        │
        ▼
    POST /api/auth/register
        │
        ▼
    Backend Validation
        │
        ├── Check email exists
        ├── Hash password (bcrypt)
        └── Create user
            │
            ▼
        Save to MongoDB
            │
            ▼
        Generate JWT Token
            │
            ▼
        Return: { user, token }
            │
            ▼
        Save to localStorage
            │
            ▼
        Redirect to Courses
```

### 2. Đăng Nhập (Login)

```
User Input
    │
    ├── Email
    └── Password
        │
        ▼
    POST /api/auth/login
        │
        ▼
    Find user by email
        │
        ▼
    Compare password (bcrypt)
        │
        ▼
    Generate JWT Token
        │
        ▼
    Return: { user, token }
        │
        ▼
    Save to localStorage
        │
        ▼
    Update App State
```

### 3. Đăng Ký Môn Học (Course Enrollment)

```
User clicks "Đăng Ký"
    │
    ▼
POST /api/courses/:id/enroll
    │
    ├── Header: Authorization: Bearer {token}
    │
    ▼
Verify JWT Token
    │
    ▼
Check Course Availability
    │
    ├── Not full?
    ├── Not enrolled?
    │
    ▼
Update Course
    │
    ├── Add student to enrolledStudents[]
    │
    ▼
Update User
    │
    ├── Add course to enrolledCourses[]
    │
    ▼
Save both to MongoDB
    │
    ▼
Return updated course
    │
    ▼
Update UI
    │
    ├── Show success message
    ├── Update button state
    └── Update course count
```

## 🗂️ Cấu Trúc Database

### Users Collection

```javascript
{
  _id: ObjectId,
  name: String,              // "Nguyễn Văn A"
  email: String,             // "student@university.edu"
  password: String,          // Hashed với bcrypt
  studentId: String,         // "SV001"
  role: String,              // "student" | "admin"
  enrolledCourses: [         // Array of Course IDs
    ObjectId,
    ObjectId,
    ...
  ],
  createdAt: Date
}
```

### Courses Collection

```javascript
{
  _id: ObjectId,
  code: String,              // "CS101"
  name: String,              // "Lập Trình Căn Bản"
  instructor: String,        // "TS. Trần Văn B"
  credits: Number,           // 3
  schedule: String,          // "Thứ 2, 7:30 - 9:30"
  room: String,              // "A101"
  maxStudents: Number,       // 40
  enrolledStudents: [        // Array of User IDs
    ObjectId,
    ObjectId,
    ...
  ],
  description: String,
  semester: String,          // "Spring 2026"
  status: String,            // "open" | "closed" | "full"
  createdAt: Date
}
```

## 🔐 Authentication Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ 1. Login (email, password)
       ▼
┌─────────────────────────┐
│   POST /api/auth/login  │
└──────┬──────────────────┘
       │
       │ 2. Verify credentials
       ▼
┌─────────────────┐
│   MongoDB       │
└──────┬──────────┘
       │
       │ 3. User found + password match
       ▼
┌──────────────────────┐
│  Generate JWT Token  │
│  Sign: {             │
│    userId: user._id  │
│  }                   │
│  Secret: JWT_SECRET  │
│  Expires: 7 days     │
└──────┬───────────────┘
       │
       │ 4. Return token
       ▼
┌─────────────────────────┐
│  Client saves token:    │
│  localStorage.setItem(  │
│    'token', token       │
│  )                      │
└──────┬──────────────────┘
       │
       │ 5. Future API calls
       ▼
┌─────────────────────────────┐
│  Add to header:             │
│  Authorization: Bearer      │
│  {token}                    │
└──────┬──────────────────────┘
       │
       │ 6. Verify token
       ▼
┌──────────────────────────┐
│  Middleware:             │
│  jwt.verify(token,       │
│    JWT_SECRET)           │
└──────┬───────────────────┘
       │
       │ 7. Token valid
       ▼
┌──────────────────────┐
│  Allow access to     │
│  protected route     │
└──────────────────────┘
```

## 📡 API Endpoints Chi Tiết

### Authentication APIs

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| POST | /api/auth/register | Đăng ký tài khoản mới | No |
| POST | /api/auth/login | Đăng nhập | No |
| GET | /api/auth/me | Lấy thông tin user hiện tại | Yes |

### Course APIs

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| GET | /api/courses | Lấy danh sách môn học | No |
| GET | /api/courses/:id | Lấy chi tiết môn học | No |
| POST | /api/courses/:id/enroll | Đăng ký môn học | Yes |
| DELETE | /api/courses/:id/unenroll | Hủy đăng ký môn học | Yes |
| POST | /api/courses | Tạo môn học mới | Yes (Admin) |
| PUT | /api/courses/:id | Cập nhật môn học | Yes (Admin) |
| DELETE | /api/courses/:id | Xóa môn học | Yes (Admin) |

### User APIs

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| GET | /api/users/profile | Lấy profile user | Yes |
| PUT | /api/users/profile | Cập nhật profile | Yes |
| GET | /api/users | Lấy danh sách users | Yes (Admin) |

## 🔧 Tech Stack Details

### Frontend Stack

```
React.js (UI Library)
    │
    ├── React Router (Routing)
    │   └── Browser-based navigation
    │
    ├── Axios (HTTP Client)
    │   └── API communication
    │
    └── CSS3 (Styling)
        └── Custom responsive design
```

### Backend Stack

```
Node.js (Runtime)
    │
    └── Express.js (Web Framework)
        │
        ├── Mongoose (MongoDB ODM)
        │   └── Schema modeling
        │
        ├── JWT (Authentication)
        │   └── Token-based auth
        │
        ├── bcryptjs (Password Hashing)
        │   └── Secure password storage
        │
        ├── CORS (Cross-Origin)
        │   └── Allow frontend access
        │
        └── express-validator
            └── Input validation
```

### Database

```
MongoDB (NoSQL Database)
    │
    ├── Document-based storage
    ├── Flexible schema
    ├── Fast queries
    └── Scalable
```

## 🌐 Deployment Architecture (Ubuntu Server)

```
┌─────────────────────────────────────────────────┐
│           VMware Virtual Machine                │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │       Ubuntu Server 20.04 LTS             │ │
│  │                                           │ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │  Node.js v18.x                      │ │ │
│  │  │  ┌───────────────────────────────┐  │ │ │
│  │  │  │   Backend Server (Port 5000)  │  │ │ │
│  │  │  │   • API Routes               │  │ │ │
│  │  │  │   • JWT Auth                 │  │ │ │
│  │  │  │   • Serve Frontend Build     │  │ │ │
│  │  │  └───────────────────────────────┘  │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  │                                           │ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │  MongoDB v7.0                       │ │ │
│  │  │  • Port: 27017                      │ │ │
│  │  │  • Database: course_registration    │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  │                                           │ │
│  │  ┌─────────────────────────────────────┐ │ │
│  │  │  Systemd Service (Optional)         │ │ │
│  │  │  • Auto-start on boot               │ │ │
│  │  │  • Log management                   │ │ │
│  │  └─────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
                    │
                    │ Network (Bridged/NAT)
                    ▼
┌─────────────────────────────────────────────────┐
│            Windows Host Machine                 │
│                                                 │
│  Browser → http://192.168.x.x:5000             │
└─────────────────────────────────────────────────┘
```

## 🔒 Security Features

1. **Password Security**
   - Hashed with bcryptjs (10 salt rounds)
   - Never stored in plain text
   - Not returned in API responses

2. **JWT Authentication**
   - Signed tokens with secret key
   - 7-day expiration
   - Validated on each protected route

3. **Input Validation**
   - Email format validation
   - Password minimum length
   - Required fields checked
   - SQL injection prevention (NoSQL)

4. **Authorization**
   - Role-based access control
   - Admin-only routes protected
   - User can only modify own data

## 📊 Performance Considerations

1. **Database Indexing**
   - Email indexed (unique)
   - Student ID indexed (unique)
   - Course code indexed

2. **Caching**
   - localStorage for user data
   - Token cached in browser

3. **Optimization**
   - Production build minified
   - Static assets served efficiently
   - Database queries optimized with populate()

---

**Tài liệu kiến trúc hoàn chỉnh! 🎯**
